using System.Collections;
using System.Collections.Generic;
using UnityEngine;
 

public class AnimacionPantallaLogo : MonoBehaviour
{
    [SerializeField]
    GameObject pantallaLogo;


    [SerializeField]
    GameObject Logo;


    // Start is called before the first frame update
    void Start()
    {
        pantallaLogo.SetActive(true);
        LeanTween.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
